import { PropertyTypeOption } from './property-type-option';

describe('PropertyTypeOption', () => {
  it('should create an instance', () => {
    expect(new PropertyTypeOption()).toBeTruthy();
  });
});
